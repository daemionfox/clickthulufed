    <div class="col">
    <nav class="navbar navbar-expand-lg navbar-dark mt-0 py-0">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse col" id="navbarSupportedContent">
                <ul class="navbar-nav mb-2 me-auto mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/@{{ comic.slug }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/@{{ comic.slug }}/archive">Archive</a>
                    </li>
                    {% if comic.currentpage.casts|length > 0 %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/@{{ comic.slug }}/cast">Cast</a>
                        </li>
                    {% endif %}

                </ul>
                <div class="d-flex">
                    <ul class="navbar-nav mb-2 me-auto mb-lg-0">
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/feed">My Feed</a>
                        </li>

                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/login">Login</a>
                        </li>
                    {% endif %}
                    </ul>
                </div>
            </div>
        </div>
    </nav>
</div>